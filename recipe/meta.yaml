{% set name = "quilt" %}
{% set pname = "quilt3" %}
{% set version = "7.0.0" %}

package:
  name: {{ pname|lower }}
  version: {{ version }}

source:
  url: https://github.com/quiltdata/{{ name }}/archive/{{ version }}.tar.gz
  sha256: ab7c6a00a21fdab4426d8f203bd782ae8e3275013cc844ab79a3e2e7f115c37a

build:
  number: 0
  noarch: python
  script:
    - cd api/python
    - {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.8
    - setuptools >=61.0
    - pip
  run:
    - python >=3.8
    - platformdirs >=2
    - aws-requests-auth >=0.4.2
    - boto3 >=1.10.0
    - jsonlines 1.2.0
    - requests >=2.12.4
    - requests-futures ==1.0.0
    - pyyaml >=5.1
    - tqdm >=4.32
    - tenacity >=5.1.1
    - jsonschema >=3,<5
    - pydantic >=2.0.0,<3.0.0

test:
  imports:
    - quilt3
  requires:
    - pip
    - setuptools >=61.0
  commands:
    - pip check

about:
  home: https://quiltdata.com/
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: 'Quilt: where data comes together'

  description: |
    Quilt is infrastructure for data-driven teams to store, version, deploy and iterate on models and data.
    Data in Quilt are visual, discoverable, and reproducible.
  dev_url: https://github.com/quiltdata/quilt

extra:
  recipe-maintainers:
    - knaaptime
    - akarve
